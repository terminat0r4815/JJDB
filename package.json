{
  "name": "mtg-ai-builder",
  "version": "1.0.0",
  "description": "MTG AI Commander Deck Idea Generator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && node build.js",
    "postbuild": "cp -r * dist/ || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.9.0",
    "dotenv": "^16.5.0",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "node-fetch": "^2.6.9"
  },
  "devDependencies": {
    "fs-extra": "^11.3.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
